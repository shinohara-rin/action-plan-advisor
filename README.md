# Action Plan Advisor

Action Plan Advisor is a dependency-free Java 21 library for deterministic bounded route advice.

The library reads immutable facts and goals. Domain providers describe methods and commands. The library never authorizes or executes commands.

## Build

```bash
./gradlew test
```

